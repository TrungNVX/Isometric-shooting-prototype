using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Utilities 
{
    public static float CalculatorStatUpgrade(this float value_,int level,float factor)
    {
       return value_ + value_ * factor * (level - 1);
    }
}
